package components.Harmony

import components.BarRhythm
import org.specs2.mutable._

class ChordTest extends Specification {

  "ChordPattern" should {
    "do something" in {



      // need to
      val rhythm = BarRhythm.rhythm(1)
      //val generator = new ChordPatternGenerator(1000)
      //c major (tonic note)
      //major - true
      // 1, 3, 5, 7, 9 +, -
      // stick with triads for now
      //val chordPattern = generator.generate(0, true, rhythm)
      //val expectedPattern = List...

      // give it a list of notes and a rhythm beforehand.

      // make it do a permutation of those notes.
      //generator.generate(chordNotes, rhythmn)
    ok
    }
  }
}